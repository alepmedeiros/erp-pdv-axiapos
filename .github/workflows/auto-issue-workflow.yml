name: Auto-Add Issues to Project Board with Labels

on:
  issues:
    types: [labeled]

jobs:
  add-to-project:
    runs-on: ubuntu-latest

    steps:
      - name: Adicionar Issue ao Quadro
        uses: muesli/action-add-project-card@v1
        with:
          project-url: https://github.com/alepmedeiros/erp-pdv-axiapos/projects/21
          column-name: >
            ${{ 
              contains(github.event.label.name, 'bug') && 'Backlog' || 
              contains(github.event.label.name, 'feature') && 'To Do' || 
              'In Progress' 
            }}
          token: ${{ secrets.GITHUB_TOKEN }}
